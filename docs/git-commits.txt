# 19-6-26:-
# Add all new, modified, and deleted files
git add .

# Create a commit with a message
git commit -m "Addons and Enhancements are on-going Adding In Deleveopment Mode Now ✅"

# Push the commit to the remote repository
git push

# Empty commit:-
git commit --allow-empty -m "Deploy and Test Now already migrations applied now"

# 19-6-26:-
# Add all new, modified, and deleted files
git add .

# Create a commit with a message
git commit -m "Addons and Enhancements are on-going Adding In Deleveopment Mode Now ✅"

# Push the commit to the remote repository
git push