(venv) PS E:\New Experiments\Flask_Blog> flask db init
Usage: flask [OPTIONS] COMMAND [ARGS]...
Try 'flask --help' for help.

Error: No such command 'db'.
(venv) PS E:\New Experiments\Flask_Blog> flask db migrate -m "Initial migration"
Usage: flask [OPTIONS] COMMAND [ARGS]...
Try 'flask --help' for help.

Error: No such command 'db'.
(venv) PS E:\New Experiments\Flask_Blog> flask db upgrade
Usage: flask [OPTIONS] COMMAND [ARGS]...
Try 'flask --help' for help.

Error: No such command 'db'.
(venv) PS E:\New Experiments\Flask_Blog>

Bro, this error:

Error: No such command 'db'

means Flask-Migrate is not registered with your Flask app OR Flask-Migrate is not installed.

Step 1: Check if Flask-Migrate is installed
pip show Flask-Migrate

If nothing is returned, install it:

pip install Flask-Migrate
Step 2: Check app/__init__.py

You should have something similar to:

from flask_migrate import Migrate
from app.extensions import db

migrate = Migrate()

def create_app():
    app = Flask(__name__)

    db.init_app(app)
    migrate.init_app(app, db)

    return app

or

migrate = Migrate(app, db)
Step 3: Verify Flask sees the db command

Run:

flask --help

You should see:

Commands:
  db
  routes
  run
  shell

If db is missing, Flask-Migrate is not properly registered.

Step 4: Check FLASK_APP
echo $env:FLASK_APP

Should point to your application, for example:

run.py

or

run
Step 5: Check imports

In run.py:

from app import create_app

app = create_app()

And inside create_app() make sure:

from flask_migrate import Migrate

is present and initialized.

Paste these 3 things:

pip show Flask-Migrate
flask --help
app/__init__.py

and I'll tell you exactly why the db command is not appearing.

Good bro ✅

Flask-Migrate is installed.

Now run:

flask --help

Paste the entire output.

Especially check whether you see:

Commands:
  db
  routes
  run
  shell

or only:

Commands:
  routes
  run
  shell

If db is missing, then Flask-Migrate is not initialized in your app.

Also run:

echo $env:FLASK_APP

and paste the output.

If db is missing, I'll need these files:

run.py
app/__init__.py

because usually the problem is one of these:

from flask_migrate import Migrate

migrate = Migrate()

missing, or

migrate.init_app(app, db)

missing, or

from app import create_app

app = create_app()

not loading the app that contains Flask-Migrate.

So next commands:

flask --help
echo $env:FLASK_APP

Paste both outputs. 🚀

Get-ChildItem

flask routes

Get-Content run.py

$env:FLASK_APP="run.py"
flask routes
flask run --host=0.0.0.0 --port=5001 --debug

Fix the current 500 error first.

Get-ChildItem .\flaskblog\logs -Recurse | Sort-Object LastWriteTime -Descending | Select-Object -First 5

Get-Content .\flaskblog\logs\2026\06\2026-06-20.log -Tail 50

Boom 💥 found it bro.

The error is crystal clear:

sqlite3.OperationalError: no such table: post

Your home() route is trying to run:

Post.query.order_by(...)

but the database does not contain the post table yet.

Why this happened

Earlier you deleted:

instance/
migrations/

After that, a new empty SQLite database got created.

So:

site.db exists ✅
post table ❌
user table ❌
password_history table ❌
First check database file
Get-ChildItem .\instance

You should see something like:

site.db
Create tables manually once

Open Flask shell:

flask shell

Then run:

from flaskblog import db
db.create_all()
exit()
Alternative (easier)

Since your run.py already contains:

with app.app_context():
    db.create_all()

just run:

python run.py

instead of:

flask run

because python run.py executes db.create_all() first.

Verify tables were created

After running:

python run.py

open:

http://127.0.0.1:5000/

(or whatever port it shows)

The 500 error should disappear.

Long-term microservice approach

For your Dedeepya-style projects:

Auth Service
Product Service
Order Service

I would not rely on:

db.create_all()

in production.

Instead use:

flask db migrate -m "Initial migration"
flask db upgrade

because when you add:

phone = db.Column(...)

later, the database schema updates safely.

Right now do this
Get-ChildItem .\instance

and

python run.py

Then tell me:

Does site.db exist?
Does /home open successfully after python run.py?

We're one step away from fixing it. 🚀

Found it bro 🎯

This is NOT your Flask code problem.

This is a Python 3.14 compatibility issue.

Look at the log:

/opt/render/project/python/Python-3.14.3/

and

AssertionError: Class SQLCoreOperations ...

This is coming from:

SQLAlchemy 2.0.27
Python 3.14.3

Your app is trying to run on Python 3.14, but SQLAlchemy 2.0.27 doesn't properly support it.

Why Render is using Python 3.14

Your build log shows:

cp314
manylinux
Python-3.14.3

which means Render ignored your intended Python version and built using 3.14.

Fix

At the root of your project create:

runtime.txt

python-3.11.9

You already uploaded a runtime file containing:

python-3.11.9

Make sure:

runtime.txt

is committed to GitHub root.

Verify

Run:

git status

You should see:

runtime.txt

tracked.

If not:

git add runtime.txt
git commit -m "Add runtime.txt for Python 3.11.9"
git push origin feature/main-testing

(or your branch)

Even better

Update SQLAlchemy:

Current:

SQLAlchemy==2.0.27

Change to:

SQLAlchemy>=2.0.41

and

Flask-SQLAlchemy>=3.1.1

But the main issue is Python 3.14.

What I would do
Keep:
runtime.txt

with:

python-3.11.9
Commit and push.
In Render:
Manual Deploy
→ Clear build cache & deploy
Watch logs.

You should then see:

Python 3.11.9

instead of:

Python 3.14.3

and the SQLAlchemy crash should disappear.

So the root cause is:

✅ Flask code okay
✅ Gunicorn command okay
✅ Requirements installed okay
❌ Render running Python 3.14.3
❌ SQLAlchemy 2.0.27 incompatible with Python 3.14

Fix the Python version first. 🚀

cat runtime.txt

Bro, we finally found the real issue. 🎯

Render is definitely still using Python 3.14.3:

/opt/render/project/python/Python-3.14.3/

and now the error changed to:

AttributeError: attribute '__default__' of 'typing.ParamSpec' objects is not writable

This is another Python 3.14 compatibility problem, not your Flask code.

Why runtime.txt isn't working

Your service is a Python 3 Native Runtime service. Render's native runtime has changed behavior, and your logs prove it is ignoring runtime.txt and building with Python 3.14 anyway.

Fastest fix

In Render Settings → Environment

Add:

PYTHON_VERSION=3.11.9

Then:

Save changes
Manual Deploy
Clear build cache & deploy
Alternative

Create a file named:

.python-version

in the repository root with:

3.11.9

Commit and push:

echo 3.11.9 > .python-version

git add .python-version
git commit -m "Force Python 3.11.9"
git push origin feature/main-testing
Also update typing_extensions

Your requirements currently pin:

typing_extensions==4.9.0

For Python 3.14 compatibility issues, bump it to:

typing_extensions>=4.15.0

because the traceback is now inside typing_extensions.py.

What I'd do right now
Add Render Environment Variable:
PYTHON_VERSION=3.11.9
Clear build cache & deploy.
If Render still uses 3.14, add:
.python-version

with:

3.11.9
Push and redeploy.

Your Flask app itself is no longer the problem. The deployment is failing before it even finishes importing SQLAlchemy because Render is launching it under Python 3.14. Once Render actually runs Python 3.11, these import-time crashes should disappear. 🚀

git log -1 --stat

If .python-version appears in the latest commit, then the Git side is completely correct.


Bro, I found the exact bug. 🎯

Your registration route is:

user = User(...)
db.session.add(user)
db.session.commit()

user.update_password_history(...)
send_verification_email(user)

flash(...)
return redirect(url_for('login'))

And your exception handler is:

except Exception as e:
    logger.error(f"Error in register route: {str(e)}")
    flash('An error occurred. Please try again later.', 'danger')

What happened

Since the database row exists:

verified = false

and registration POST returned:

POST /register HTTP/1.1" 200

the failure happened after:

db.session.commit()

Most likely at:

send_verification_email(user)

Another problem I found

Your email sender is:

msg = Message(
    'Email Verification',
    sender='noreply@demo.com',
    recipients=[user.email]
)

But your Flask config uses:

MAIL_USERNAME = practicesession3@gmail.com
MAIL_DEFAULT_SENDER = MAIL_USERNAME

So Gmail authenticates as:

practicesession3@gmail.com

but you're trying to send as:

noreply@demo.com

Many SMTP providers reject that.

Fix #1 (recommended)

Change BOTH email functions.

Current:

msg = Message(
    'Email Verification',
    sender='noreply@demo.com',
    recipients=[user.email]
)

Change to:

msg = Message(
    'Email Verification',
    recipients=[user.email]
)

or:

msg = Message(
    'Email Verification',
    sender=app.config["MAIL_DEFAULT_SENDER"],
    recipients=[user.email]
)

Do the same for:

send_reset_email()

Fix #2 (VERY IMPORTANT)

Add logging around email sending.

Replace:

mail.send(msg)

with:

try:
    mail.send(msg)
    logger.info(f"Verification email sent to {user.email}")
except Exception as e:
    logger.error(f"EMAIL ERROR: {str(e)}")
    raise

Then redeploy.

Fix #3

Check Gmail App Password.

You MUST use:

Google Account
→ Security
→ 2-Step Verification
→ App Passwords

and store the 16-character App Password in:

MAIL_PASSWORD

Not your Gmail login password.

Quick proof test

Try login now.

Your database record shows:

verified = false

So login should show:

Your email is not verified. Please check your email.

because:

if user.verified:
    login_user(...)
else:
    flash(
      'Your email is not verified. Please check your email.'
    )

If that happens, it proves:

✅ Registration works
✅ User saved
✅ Password hash works
✅ Login works
❌ Email verification send fails

Next command

Open Render Shell and inspect the latest application log file for the actual email exception, or make the sender change above and redeploy. My bet is the SMTP rejection is caused by:

sender='noreply@demo.com'

while authenticating as:

practicesession3@gmail.com

That's the strongest bug visible in the code you uploaded. 🚀

(venv) PS E:\New Experiments\Flask_Blog> python -m py_compile flaskblog/routes.py
(venv) PS E:\New Experiments\Flask_Blog> 

and it returns immediately with no output and no errors, it means:

✅ The file has valid Python syntax
✅ Indentation is correct
✅ All blocks (try, except, functions, routes, decorators, etc.) are properly structured
✅ Python was able to compile the file into bytecode successfully

